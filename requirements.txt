# Pin exact-ish ranges; regenerate a lockfile (pip-compile / uv pip compile) for prod.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
gunicorn>=22.0,<24.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
hvac>=2.3,<3.0
cryptography>=42,<46
slowapi>=0.1.9,<0.2
prometheus-client>=0.20,<1.0

# dev/test only
pytest>=8.0
httpx>=0.27
